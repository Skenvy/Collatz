name: Python 🐍🐍🐍 Tests 🦂
on:
  push:
    branches-ignore:
    - 'main'
    paths:
    - 'python/**'
    - '!python/**.md'
    - '.github/workflows/python-*'
  pull_request:
    branches:
    - 'main'
    paths:
    - 'python/**'
    - '!python/**.md'
    - '.github/workflows/python-*'
  workflow_call:
    inputs:
      development_python_version:
        description: 'The version of python used in development'
        default: '3.8.10'
        required: false
        type: string
permissions: {}
defaults:
  run:
    shell: bash
    working-directory: python
jobs:
  quick-test:
    name: Python 🐍🐍🐍 Quick Test 🦂
    if: ${{ github.event_name == 'push' && !(github.event.ref == 'refs/heads/main') }}
    runs-on: ubuntu-latest
    steps:
    - name: 🏁 Checkout
      uses: actions/checkout@93ea575cb5d8a053eaa0ac8fa3b40d7e05a33cc8 # v3.1.0
    - name: 🐍🐍🐍 Set up Python
      uses: actions/setup-python@13ae5bb136fac2878aff31522b9efb785519f984 # v4.3.0
      with:
        python-version: ${{ inputs.development_python_version }}
    - name: 🧱 Install build dependencies
      run: make venv
    - name: 🦂 Test with pytest
      run: make test
    - name: 🧹 Lint
      run: make lint
  full-test:
    name: Python 🐍🐍🐍 Full Test 🦂
    if: >- 
      ${{ github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && github.event.ref == 'refs/heads/main') }}
    runs-on: '${{ matrix.os }}'
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.6', '3.7', '3.8', '3.9', '3.10']
        os: [ubuntu-20.04, macOS-latest, windows-latest]
    steps:
    - name: 🏁 Checkout
      uses: actions/checkout@93ea575cb5d8a053eaa0ac8fa3b40d7e05a33cc8 # v3.1.0
    - name: 🐍🐍🐍 Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@13ae5bb136fac2878aff31522b9efb785519f984 # v4.3.0
      with:
        python-version: ${{ matrix.python-version }}
    - name: 🍑 Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install tox tox-gh-actions
    - name: 🦂 Test with tox
      run: tox
    - name: 🧹 Lint
      run: make lint
  codeql:
    name: Python 🐍🐍🐍 CodeQL 🛡👨‍💻🛡
    if: >- 
      ${{ github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && github.event.ref == 'refs/heads/main') }}
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: ./.github/workflows/github-codeql.yaml
    with:
      language: 'python'
