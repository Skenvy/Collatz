{
  "name": "demo",
  "version": "1.0.0",
  "description": "Demonstration",
  "license": "Apache-2.0",
  "main": "index.js",
  "dependencies": {
    "@skenvy/collatz": "file:./skenvy-collatz.tgz"
  },
  "scripts": {
    "strip-version": "[ -f skenvy-collatz.tgz ] || mv skenvy-collatz-*.tgz skenvy-collatz.tgz",
    "prepare": "npm run strip-version",
    "build": "tsc main.ts",
    "demo": "node main.js",
    "everything": "npm i && npm run build && npm run demo"
  },
  "devDependencies": {
    "@types/node": "^20.3.2",
    "typescript": "5.1.5"
  }
}
